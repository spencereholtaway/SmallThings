{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "ignoreCommand": "git diff --name-only HEAD~1 HEAD | grep -v '^entries\\.json$' | grep -q . && exit 0 || exit 1",
  "rewrites": [
    { "source": "/entries(.*)", "dest": "/api/index.js" },
    { "source": "/health", "dest": "/api/index.js" }
  ]
}
